{
  "name": "jules-ai-workspace",
  "private": true,
  "workspaces": ["apps/web", "services/artifact-renderer"],
  "scripts": {
    "dev": "npm run dev:web",
    "build": "npm run build:web",
    "lint": "npm run lint:web",
    "test": "npm run lint:web && npm run test:web && npm run build:web && npm run test:api",
    "dev:web": "npm --workspace apps/web run dev",
    "build:web": "npm --workspace apps/web run build",
    "lint:web": "npm --workspace apps/web run lint",
    "test:web": "npm --workspace apps/web run test",
    "test:api": "cd services/api && uv run pytest -q",
    "dev:api": "cd services/api && uv run uvicorn app.main:app --reload --port 8000 --no-access-log",
    "dev:worker": "cd services/api && uv run python -m app.knowledge_worker",
    "dev:artifact-worker": "cd services/api && uv run python -m app.artifact_worker"
  }
}
